class Subject {
  final String id;
  final String intitule;

  Subject(this.id, this.intitule);

  /*factory Subject.fromJson(Map<String, dynamic> json) {
    return Subject(
      title: json['intitule'],
    );
  }*/

}